{
let form = $("<%= escape_javascript render 'data_form_entity_response_group_rsvp_email_form' %>");
let modal = $('<%= escape_javascript render 'email_modal' %>');
$('#modal-mail-form').remove();
$(' .modal-title', modal).html("Enviar correo electrónico con pase de entrada");
$(' .modal-body', modal).empty().append(form);
$('#modal-mail-form').remove();
$(modal).attr('id', 'modal-mail-form');
$('body').append(modal);
tinyMCE.init({
selector: '.tinymce.editor'
});
modal.modal('show');
}